const accordeon = (wrapperQuery, itemsQuery) => {

    if(wrapperQuery && itemsQuery) {

        const wrapper = document.querySelector(wrapperQuery);
        const items = wrapper.querySelector(itemsQuery);

        if(items?.length > 0) {

        }
    }

}

export default accordeon;